# auto-downloader 스펙

> 이 파일이 이 프로젝트의 "진짜 요청서"다.
> Claude에게 말로 길게 설명하는 대신 여기를 채우고 "SPEC.md 보고 진행해"라고 하면 된다.

## 목적
친구가 영상편집 소스를 받을 때 쓴다. URL을 넣으면 자동으로 다운로드되고,
여러 URL을 한 번에 넣어도 병렬로 동시에 받는다.
유튜브뿐만 아니라 인스타그램, 틱톡, 샤오홍슈, 도우인 영상도 받는다.

## 입력 / 출력
- 입력: 웹 화면에 URL 붙여넣기 (여러 개는 줄바꿈으로 구분). `실행.command` 더블클릭하면 브라우저가 자동으로 열린다.
- 출력 위치: 기본 `~/Downloads/영상소스/`, 화면의 "저장 위치 바꾸기" 버튼으로 변경 가능 (settings.json에 저장, 컴퓨터마다 따로 기억)
- 파일명 규칙: `{업로드날짜}_{제목}.mp4` (업로드날짜를 알 수 없으면 받은 날짜)

## 규칙
- 화질: **무조건 원본 최고 화질** (영상+소리 최고 품질을 합쳐서 mp4로)
- 자막: 영상에 자막 데이터가 있으면 같이 가져온다 (수동 자막 > 자동 자막, 한국어 > 영어).
  화면에서 타임스탬프와 함께 한 줄씩 보여주고, "자막만 복사" / "타임스탬프 포함 복사" 버튼 제공.
  단, 화면에 구워진(영상에 새겨진) 자막은 데이터가 아니라서 못 가져옴
- 병렬: 동시에 3개씩 다운로드 (사이트 차단 방지를 위한 제한, 나머지는 자동 대기)
- 다운로드 실패 시: 3번 재시도 후 건너뛰고, 화면에 실패 목록과 한국어 이유 표시
- 로그인이 필요한 영상(비공개 인스타 등)은 1차 버전에서는 지원하지 않음

## 완료 기준 — 이게 다 되면 "완성"
- [x] 친구가 더블클릭하면 프로그램이 뜬다
- [x] URL을 넣으면 다운로드가 된다
- [x] 실패해도 죽지 않고, 한국어로 이유를 알려준다
- [x] docs/에 친구가 읽을 수 있는 사용 설명서가 있다

## 할 일
> 하나 끝날 때마다 체크하고 커밋한다. (나중에 랄프 루프를 돌릴 때도 이 목록을 그대로 쓴다)

- [x] 프로젝트 뼈대: venv + requirements.txt (flask, yt-dlp) + ffmpeg 확인 → ffmpeg는 static-ffmpeg 패키지로 venv에 내장 (친구 컴퓨터에 별도 설치 불필요)
- [x] 다운로드 엔진: 여러 URL 병렬(3개 동시), 원본 최고화질 mp4, 3회 재시도, 한국어 에러 메시지
- [x] 웹 화면: URL 붙여넣기 → 시작 → 진행률/완료/실패 목록 표시, 폴더 열기 버튼
- [x] 실행.command (더블클릭 → 서버 실행 + 브라우저 자동 오픈), 처음-설치.command
- [x] 실제 URL로 다운로드 테스트 → 2026-07-31 유튜브 19초 영상으로 확인, `2005-04-24_Me at the zoo.mp4` 생성됨. 실패 URL 2종도 한국어 메시지 확인
- [x] docs/ 한국어 사용 설명서 (docs/사용법.md)

- [x] 틱톡 실제 URL 테스트 → 처음엔 봇 차단으로 실패. 실패 시 브라우저 흉내(impersonate) 모드로 자동 재시도하게 고쳐서 1080p 다운로드 성공 (2026-07-31)
- [x] 여러 URL 병렬 + 같은 영상 재다운로드 시 "(2)" 붙는 것 확인
- [x] 친구 실사용에서 틱톡 실패 재발 (2026-08-01) → 원인: 틱톡 봇 차단이 간헐적인데 영상 하나에 페이지를 2번 접속하는 구조라 실패 확률이 2배였음. 접속 1번으로 줄이고(정보 재활용), 봇 차단 사이트는 크롬/사파리 흉내를 바꿔가며 최대 5회 재시도. 친구가 실패한 영상으로 검증 완료
- [x] 실행.command가 매번 git pull + yt-dlp 업데이트 → 앞으로 수정사항은 친구가 재실행만 하면 반영됨
- [x] 처음-설치.command 끝에 설치 검증 단계 추가 (부품 빠지면 한국어로 안내)
- [x] 저장 위치 바꾸기 버튼 (맥 폴더 선택창, settings.json에 기억) (2026-08-01)
- [x] 자막 기능: 다운로드 완료 시 자막도 가져와 타임스탬프와 함께 표시 + 전체 복사 버튼 (2026-08-01, 유튜브 자막으로 검증)
- [x] 인스타그램 공개 게시물 다운로드 확인 (2026-08-01, yt-dlp 테스트 게시물로 검증)
- [x] 틱톡샵 연결 영상이 mp3(소리만)로 받아지던 문제 (2026-08-01)
  → 원인: 틱톡샵 영상은 틱톡 웹페이지 데이터에 영상 주소가 아예 없음 (yt-dlp 미해결 이슈 #13928).
     yt-dlp가 소리 트랙만 찾아서 mp3가 됨. 웹 API·앱 API·임베드 페이지 다 확인했지만 전부 영상 주소 없음
  → 해결: 영상 트랙이 없으면 공개 API(tikwm.com)를 거쳐 mp4를 받는 안전망 추가.
     흔한 다운로드 사이트들이 쓰는 방식. 1080x1920 mp4 다운로드 + ffprobe로 영상 트랙 검증 완료
- [x] 음성인식 자막 만들기 (2026-08-01)
  → faster-whisper(small, CPU) 사용. 내 컴퓨터에서 처리되고 무료, 인터넷 전송 없음
  → 자막 없는 영상에 "🎧 소리 듣고 자막 만들기" 버튼. 88초 인도네시아어 영상 → 40줄 인식 검증 완료
  → 모델은 처음 쓸 때 한 번만 받음(~500MB), 그 뒤로는 재사용

## 안정성 수정 (2026-08-01 코드 점검에서 확인된 버그들)
> 전부 "조용히 잘못된 결과를 주는" 것들. 실패가 눈에 보이면 친구가 연락이라도 하지만
> 이것들은 초록색 "완료"가 뜨거나 영원히 "대기 중"에 멈춰서 아무도 모른다.

- [x] A. 받다가 끊긴 파일을 "완료"로 표시 — 틱톡샵 우회 경로에 크기 검증이 없음
- [x] B. 음성인식을 한 번에 하나씩 — 지금은 버튼 누른 수만큼 스레드가 떠서 맥 전체가 느려짐
- [x] C. 저장 폴더가 없으면(외장하드 뺐을 때) 에러 없이 영원히 "대기 중" — mkdir이 try 밖
- [x] D. 공개 영상을 "비공개"라고 잘못 안내 — 유튜브 봇 차단 문구 "Sign in to confirm you're not a bot"이
       재시도 불가로 분류돼서, 틱톡 때문에 만든 재시도 사다리가 통째로 건너뛰어짐
- [x] E. 자막이 한 번 실패하면 재시작 전까지 영영 다시 못 만듦 (버튼이 사라지고 API도 무반응)
- [x] F. 자막을 영상 옆 .txt로도 저장 — 지금은 터미널 닫으면 음성인식 결과가 그냥 사라짐
- [x] G. 제목에 [4K] 같은 대괄호가 있으면 받아놓고도 "파일을 찾지 못했어요" 오류
- [x] H. 서버가 꺼져도 화면은 "진행 중"으로 얼어붙어 있음 → 꺼진 걸 알려주기
- [x] I. 1초마다 화면을 다시 그려서 자막 읽는 중 스크롤이 맨 위로 튐

## 편집용 파일 만들기 (캡컷)
> 친구는 캡컷을 쓴다. 캡컷은 HEVC는 잘 읽지만 가변 프레임에 취약하다 —
> 타임라인이 고정 프레임을 가정해서, 미리보기는 맞는데 내보내기하면 소리가 어긋난다.
> 받은 파일 4개 중 1개(틱톡 슬라임)가 실제로 가변 프레임이었다 (30fps/20fps를 오감).

- [x] 다운로드 완료 시 가변 프레임인지 자동 감지 (ffprobe, 10ms 미만)
      → 실제 파일 4개로 검증: 슬라임(틱톡)만 가변 판정, 나머지 3개는 건드리지 않음
- [x] 가변 프레임일 때만 "🛠️ 편집용 파일 만들기" 버튼 + 소리 밀림 경고
- [x] 원본은 그대로 두고 `{이름}_편집용.mp4`를 옆에 하나 더 만듦
      → 검증(2026-08-01): 60/24.2 가변 → 30/30 고정으로 변환, 소리 밀림 0.051초(무시 가능), 10MB→50MB
- [x] 코덱 변환(HEVC→H.264)은 안 함 — 캡컷이 잘 읽으므로 불필요. 프리미어로 바꾸면 그때

## 다음에 하면 좋은 것 (아직 안 함)
- [ ] 도우인 실제 URL로 테스트 (친구가 실제 쓰는 URL로 해보는 게 정확함)
- [ ] 로그인 필요한 영상 지원 (브라우저 쿠키 연동)
- [ ] 음성인식이 느리거나 부정확하면 모델 크기 조절 (small → base는 빠르고 덜 정확, medium은 반대)

## 아이콘 앱 (2026-08-01)
> 터미널 검은 창을 계속 띄워둬야 하는 게 불편하다는 요청.

- [x] `영상 다운로더.app` — 아이콘 더블클릭으로 터미널 없이 백그라운드 실행 + 브라우저 자동 열림
- [x] 이미 켜져 있으면 서버를 새로 띄우지 않고 화면만 다시 열기
- [x] 끄는 법 두 가지: 화면의 "🔴 프로그램 끄기" 버튼, 독 아이콘 우클릭 → 종료
- [x] 앱이 사라지면 서버도 같이 종료 (서버가 WATCH_APP_PID를 지켜봄)
- [x] 아이콘 직접 제작 (tools/아이콘만들기.py), 앱 빌드 (tools/앱만들기.sh)
- 앱을 고쳤을 때: `bash tools/앱만들기.sh` 를 다시 돌려야 반영된다

### 삽질 기록 — 독에서 종료가 안 되던 문제
서버를 `nohup ... &` 로만 띄우면 **서버가 앱의 입출력을 붙잡고 있어서** 독에서 종료해도
앱이 안 꺼졌다. 원인을 찾느라 최소 예제를 단계별로 만들어 비교해야 했다.
`( ... & ) &` 처럼 **서브셸로 한 겹 더 감싸 완전히 떼어내야** 한다.
같이 확인된 것: `on quit`이나 `on idle` 안에서 `do shell script`를 부르면 종료가 먹통이 된다.

## 친구 실사용 오류 (2026-08-01)
- [x] `Impersonate target "safari" is not available` — 친구 컴퓨터의 curl_cffi가 사파리 흉내를 지원 안 함
  → 원인: 흉내낼 브라우저 이름을 코드에 박아뒀는데, 지원 목록은 curl_cffi 버전마다 다르다
  → 해결: 켤 때 실제로 쓸 수 있는 목록을 읽어와서 그 중에서만 고른다.
     혹시 안 되는 게 섞여 있으면 목록에서 빼고 다음 것으로 넘어간다 (그 오류를 최종 오류로 보여주지 않음)
  → yt-dlp 내부 API를 쓰므로, 그게 바뀌면 chrome만 쓰도록 안전장치도 넣음
  → 검증: 없는 흉내를 일부러 끼워넣어 자동으로 빠지는 것 확인, 유튜브·틱톡 회귀 통과

- [x] 받은 영상이 파인더에서 안 틀어지는 문제 (2026-08-01)
  → 원인: 합치기에 실패하면 .mkv/.webm으로 저장됐고(우리 코드가 그걸 허용했음),
     유튜브 고화질은 AV1·VP9·Opus로 오는데 맥 버전이나 기종에 따라 이걸 못 연다.
     개발 맥(macOS 26, 애플실리콘)은 AV1도 열려서 재현이 안 됐다 — 친구 맥이 더 낮은 사양일 가능성
  → 해결: 받은 뒤 자동 검사해서, 그릇만 문제면 다시 담고(무손실·수초),
     코덱이 문제면 h264+aac로 변환한다. 못 여는 원본은 지워서 헷갈리지 않게 함
  → 검증: 안 열리는 파일 3종(mkv, vp9+opus webm, opus mp4)을 만들어
     고치기 전 ❌ → 고친 뒤 ✅ 를 파인더 미리보기(QuickLook)로 확인

## 결정 기록
- **웹 배포는 하지 않는다** (2026-08-01 검토) → [docs/웹배포-검토.md](docs/웹배포-검토.md)
  - 클라우드 IP는 유튜브·틱톡이 상시 차단해서 기술적으로 오히려 더 안 됨
  - 로컬 도구(사적이용)와 서비스 제공(OSP)은 법적으로 완전히 다른 영역
  - 폴더 열기·저장 위치 바꾸기·자막 .txt 등 지금 기능 절반이 웹에선 못 씀
  - 별건으로 확인 필요: 친구가 받은 영상을 결과물에 넣어 업로드하는지 (그건 배포와 무관한 별개 문제)

## 알아둘 위험
- 틱톡샵 영상의 mp4 안전망은 외부 공개 API(tikwm.com)에 의존한다. 그 서비스가 멈추면 이 경우만 다시 mp3가 된다.
  yt-dlp 이슈 #13928이 고쳐지면 안전망 없이도 되므로 그때 걷어내도 됨
- tikwm.com이 간헐적으로 클라우드플레어 차단을 켠다 (2026-08-10 확인: 오전엔 되다가 오후에 403).
  틱톡 사이트 안 검색은 이때 "새 탭으로 검색 ↗" 버튼으로 자동 대체되므로 프로그램은 안 죽는다.
  틱톡 데이터에 기대는 새 기능을 만들 땐 반드시 "안 될 때 경로"를 같이 설계할 것

## 새 기능 제안 — 소스 찾기·정리 도우미 (2026-08-09, 아직 시작 안 함)
> 친구의 실제 루틴: 상품 키워드를 정함 → 영어·중국어로 번역해 틱톡에서 검색 → 남의 영상을 보며
> 파생 키워드 발견(예: "스텐 얼룩 제거제" → "레인보우 얼룩") → 그 키워드로 재검색해 다운로드
> → 못 찾으면 쿠팡 리뷰에서 키워드 발굴 → 유튜브·틱톡·샤오홍슈에서 검색
> → 쓸만해 보이는 영상 전부 다운받고 나중에 확인 → 용량 부족 + 중복 영상 때문에 지우는 작업 반복.
>
> 사이트가 줄여줄 수 있는 수작업: ① 번역해서 각 사이트에 검색어 넣는 일
> ② 키워드 캐내는 일 ③ 중복·용량 정리하는 일

화면 구성: **지금 화면은 그대로 두고**, 맨 위에 탭 3개로 나눈다.
`[⬇️ 다운로드 (지금 화면)] [🔍 키워드 찾기] [🗂 보관함 정리]`

### 탭 2 — 키워드 찾기 (2026-08-09 구현)
- [x] 한국어 키워드 입력 → 영어·중국어 자동 번역 (deep-translator, 무료·API키 불필요)
      → 검증: "스텐 얼룩 제거제" → "stainless steel stain remover" / "不锈钢去污剂"
- [x] 언어별 원클릭 검색 버튼 (새 탭): 한국어→쿠팡·유튜브·틱톡·인스타 / 영어→틱톡·유튜브·인스타 /
      중국어→샤오홍슈·도우인·틱톡. 언어마다 실제로 쓰는 사이트만 보여줌
- [x] 연관 키워드 추천: 유튜브 자동완성 API (무료, 키 불필요) — 언어별 칩, 누르면 그 말로 재검색
      → 검증: "스텐 얼룩" → "스텐 냄비 무지개 얼룩" 등 7건 (친구가 손으로 찾던 파생 키워드가 자동으로 나옴)
- [x] 사이트 안 검색 — 유튜브: yt-dlp ytsearch, 썸네일·제목·길이·조회수 그리드,
      썸네일 클릭 → 그 자리에서 재생(임베드), "⬇️ 받기" → 기존 다운로드 엔진으로 전달
      → 검증: 9건 검색, 브라우저 렌더링 확인 (Playwright 스크린샷)
- [x] 사이트 안 검색 — 틱톡: tikwm.com 검색 API, 세로 썸네일 그리드, 클릭 → mp4 바로 재생
      → 검증: 12건 검색, 사이트 안 재생 확인, 검색에서 찾은 영상을 "받기"로 실제 다운로드 완료까지 확인
- [ ] 받은 영상에서 키워드 캐내기: 다운로드 완료 항목에 제목·해시태그·설명 속 키워드를 칩으로 표시,
      누르면 그 키워드로 각 사이트 재검색 (기존 다운로드 화면을 건드려야 해서 이번엔 뺌 — 다음에)
- [x] 쿠팡 리뷰 붙여넣기 분석: 자주 나온 단어 순위, 단어 클릭 → 그 말로 검색.
      조사 떼기 + 상투어(배송·추천·좋아요 등) 제거
      → 검증: 샘플 리뷰에서 "냄비·무지개·얼룩·기름때"만 남고 잡음 걸러짐
- [x] 화면: 기존 화면은 코드 그대로 두고 탭으로 감싸기만 함. 탭 전환·재생 창·토스트 추가.
      틱톡 검색이 죽으면 "새 탭으로 검색 ↗" 버튼으로 자동 대체

### 탭 3 — 보관함 정리
- [ ] 폴더 현황: 받은 파일 목록(크기·날짜 정렬), 전체 사용 용량, 노트북 남은 공간 표시
- [ ] 완전 중복 찾기: 내용이 똑같은 파일을 묶어서 보여주고 하나만 남기고 지우기
- [ ] 의심 중복 찾기: 길이·해상도가 같은 파일을 썸네일로 나란히 비교시켜주고 선택 삭제
- [ ] 삭제는 전부 macOS 휴지통으로 이동 (실수해도 복구 가능)
- [ ] 선별 모드: 브라우저에서 영상을 바로 재생하면서 "남기기 / 지우기" 버튼으로 빠르게 정리
      (안 열리는 코덱은 이미 h264로 자동 변환하므로 대부분 브라우저 재생 가능)
- [ ] 다운로드할 때 "이미 받았던 영상이에요" 경고: 받은 영상 기록을 기억해서 같은 걸 또 받기 전에 알려줌

### 안 되는 것 / 안 하는 것 (정직하게)
- 샤오홍슈·도우인 검색결과를 사이트 안에 띄우기 — 로그인 + 봇 차단 때문에 불안정.
  번역된 검색어로 검색 페이지를 새 탭으로 여는 것까지만 (yt-dlp도 개별 영상만 지원, 검색은 미지원 확인)
- 쿠팡 리뷰 자동 수집 — 쿠팡 봇 차단이 강력함. 붙여넣기 분석으로 대체
- 인스타그램 검색 개선 — 인스타 자체의 한계라 해결 불가. 번역 키워드로 검색 페이지 열어주는 것까지만
- 틱톡 사이트 안 검색은 tikwm.com 의존이 하나 더 늘어나는 것 — 그 서비스가 멈추면 이 기능만 꺼지고,
  검색 페이지 새 탭 열기로 자동 대체되게 만든다. 무료라 요청 속도 제한(초당 1회 수준)도 있음

## 새 기능 제안 2 — 핫 소재 찾기 + 원본 찾기 (2026-08-10 조사, 아직 시작 안 함)
> 친구의 추가 요청: ① 지금 뜨는(핫한) 영상을 조건으로 찾고 싶다 — 예: "올린 지 8시간 안에
> 댓글 1000개 넘으면 핫한 것" — 기준은 가변으로, 결과는 순위대로. ② 원본 영상 찾기도 중요.
> 지금은 머닝(moneying.biz)이라는 유료 사이트를 쓰는데 기능마다 돈을 내야 해서 부담.

### 머닝(moneying.biz) 분석 (2026-08-10)
크리에이터 수익 관리 플랫폼. 3개 제품(머닝링크/트래커/도구)을 따로 구독하는 구조.
친구에게 필요한 건 "트래커" — 급상승 영상 자동 감지(업로드 5시간 내 3만 뷰 = 급등, 기준 고정),
채널 벤치마킹, 키워드 트렌드, AI 대본 추출, 교차영상 검색(링크 → 틱톡·인스타·샤오홍슈에서
원본/유사 영상 찾기, 월 50~200건). 이 풀세트가 트래커 PLUS 월 29,000원.

우리가 만들 수 있는 것 / 없는 것:
- 급상승 영상 감지(키워드 기반) → ✅ 만들 수 있음. 게다가 머닝은 기준이 고정인데 우리는 가변 가능
- AI 대본 추출 → ✅ 이미 있음 (자막 가져오기 + 음성인식 자막)
- 교차영상 검색(원본 찾기) → 🔶 반자동 버전 가능 (아래). 완전 자동 매칭은 영상 지문 DB가 필요해서 불가
- 채널 벤치마킹(등록 채널 상시 감시) → 🔶 가능하지만 프로그램이 켜져 있을 때만. 나중 단계로
- 인스타그램 급등 감지 → ❌ 로그인·차단 때문에 불가
- 쿠팡 파트너스 링크 도구 → ❌ 우리 프로젝트 대상 아님

### 🔥 핫 영상 찾기 — 기술 검증 완료 (2026-08-10)
- 유튜브 ✅ 완전 가능:
  1. 기간 필터(1시간/오늘/이번 주) + 조회수순 검색 — /results URL의 sp 파라미터 (검증: 오늘+조회수순
     `sp=CAMSAggC`로 8건, 전부 24시간 안 영상)
  2. 상위 N개 정밀 조회 → 업로드 시각(timestamp)·조회수·댓글수·좋아요 다 나옴
     (검증: 1개당 1.0초, 3개 동시 처리로 6개 2.2초)
  3. "올린 지 X시간"과 시간당 조회수 계산 → 조건 필터 → 순위표
- 틱톡 🔶 조건부: tikwm 검색 응답에 create_time·댓글·좋아요가 포함되는 것으로 알려져 있으나
  오늘 클라우드플레어 차단 때문에 실측 미완 — 차단 풀리면 확인하고 진행.
  안 될 때는 유튜브 결과만 보여주고 틱톡은 새 탭 버튼으로 대체
- 조건 설정 UI: 기간 / 최소 조회수 / 최소 댓글수 / 정렬 기준(시간당 조회·총 조회·댓글)을
  화면에서 바꾸게 하고 settings.json에 기억 (친구가 말한 "가변 기준")

- [ ] 조건 설정 UI + settings.json 저장
- [ ] 유튜브 핫 영상 검색 (기간 필터 → 정밀 조회 3개 동시 → 조건 계산 → 순위표)
- [ ] 순위표 화면: 올린 지 몇 시간, 조회수, 시간당 조회수, 댓글, 좋아요 + 기존 재생/받기 버튼 재사용
- [ ] 틱톡 핫 영상 (tikwm 차단 풀린 뒤 필드 실측 → 되면 같은 순위표에 합류)

### 🔎 원본 영상 찾기 — 반자동 버전 설계
머닝의 교차영상 검색처럼 "링크 넣으면 자동 매칭"은 못 하지만, 이 흐름은 만들 수 있다:
- [ ] 영상 링크 붙여넣기 → yt-dlp로 제목·해시태그·설명·음악 정보 추출 (다운로드 없이, 1초)
- [ ] 추출된 키워드를 자동 번역해서 기존 사이트 안 검색(유튜브·틱톡)에 바로 던짐
      + 샤오홍슈·인스타·도우인은 새 탭 검색 버튼
- [ ] 검색 결과를 "오래된 순"으로도 정렬 가능하게 → 제일 오래된 게 원본일 가능성이 큼
- [ ] 틱톡 음악 정보가 "original sound - 계정명"이면 그 계정이 원본 제작자일 가능성 표시
- [ ] 프레임 캡처 도우미: 영상에서 대표 장면을 뽑아 저장해주고 구글 렌즈를 열어줌
      (이미지를 끌어다 놓으면 역검색 — 이 단계만 수동)

### 🔍 인스타 급등 감지 조사 (2026-08-10) — 로그인 없이는 불가 판정
친구의 진짜 요구는 "인스타에서 뜬 영상 찾기" (유튜브는 불필요하다고 함). 익명 접근을 전부 실측:
- 인스타 공식 웹 API (프로필 web_profile_info): 400 에러 — 로그인 필수로 바뀜
- 인스타 해시태그 API: 로그인 페이지로 돌려보냄
- 미러 사이트 4곳 (tikwm 같은 중계 사이트): pixwox 해시태그는 열리지만 게시물 상세가 CF 403,
  imginn은 열리지만 좋아요·댓글 숫자를 안 보여줌, picnob·pixwox 프로필은 CF 차단.
  → 핵심인 "좋아요·댓글·업로드시각" 통계를 익명으로 얻을 길이 없음
- 머닝이 인스타 감지를 유료로 파는 이유가 이것 — 로그인 세션을 서버에서 돌려야 가능한 영역

가능한 유일한 길: **로그인 세션 기반** (인스타 ToS 위반 영역이라 계정 일시제한 위험 있음 → 부계정 필수)
- 부계정 로그인(쿠키 또는 ID/PW) → 해시태그·벤치마킹 계정의 최근 릴스 + 좋아요·댓글·시각 수집
- 가변 기준(N시간 내 좋아요·댓글 X개) 필터 → 순위표 → 기존 "받기" 연결
- 요청량을 사람 수준으로 제한 (캐시 10분, 순차 요청, 하루 수십 회 수준)
- [ ] 진행 여부는 사용자 결정 대기 — 부계정 준비 + 위험 감수 동의가 선행 조건

### ⚖️ 인스타 정지 위험 검증 (2026-08-10) — 두 갈래로 결론이 갈림
친구 요청: "만들기 전에 계정 정지·불이익 위험부터 검증". 공식 문서 + 실사례(instagrapi 이슈·레딧) 조사 결과:

**A. 비공식 방식 (부계정 로그인 세션으로 수집) — 위험 실재. 추천 안 함**
- "읽기 전용·저볼륨(하루 20~60)"이어도 안전하지 않음. 걸리는 건 볼륨이 아니라 자동화 지문·재로그인 패턴
- 일시 제한(24~48h): 높음(사실상 상시 — 세션이 주당 5~15% 탐) / 셀피·전화 인증 요구: 중~높음(특히 새 부계정은 14일 내 거의 다 걸림) / 부계정 영구정지: 중간(누적) / **본계정 전이: 격리 안 하면 중간 이상** (기기지문·전화번호·Accounts Center로 묶으면 연쇄 정지 실증됨, 2025 밴 물결)
- 개인이 소송당한 사례는 없음(소송은 전부 대량·상업). 위험은 "계정 조치"에 국한
- instagrapi 메인테이너 본인도 "라이브러리로 안전하게 만드는 스위치는 없다"고 인정

**B. 공식 API (Business Discovery, Facebook Login) — 계정 정지 위험 사실상 없음. 이 길로 가야 함**
- 약관 안에서 Meta가 허가한 채널이라 친구 계정 정지 위험 없음(벌점은 계정이 아니라 "앱"에 귀속)
- Business Discovery로 벤치마킹 프로페셔널 계정들의 릴스 → like_count·comments_count·timestamp·permalink 수집 가능
  (개수 제한 없음, Standard Access라 앱 심사 없이 개발 모드 + 본인 계정으로 동작 — 가장 확실한 경로)
- **설정 필요**: 인스타 계정을 프로페셔널로 전환 → 페이스북 페이지 연결 → Meta 개발자 앱 생성 → 토큰 발급
  (비개발자 친구에겐 이 초기 설정이 벽. 60일마다 토큰 재발급 이벤트도 있음 → 페이지 토큰으로 완화)
- **포기할 것(정직하게)**: ① 아무 계정이나 감시 불가 — 상대도 프로페셔널 계정이어야 함
  ② 해시태그 검색은 작성자(username) 안 줌 + 최근 24시간 창 + 주 30개 제한이라 "인스타 전체에서 뜨는 거 찾기"는 약함
  → 즉 공식 API는 "미리 정한 벤치마킹 계정 목록의 신작 릴스 순위표"에는 강하지만,
     "인스타 전체 열린 탐색"은 약함. 친구가 어떤 쪽을 원하는지 확인 필요
- 남은 불확실성: 해시태그 검색이 개발 모드에서 심사 없이 되는지는 Graph API Explorer로 15분 실측 먼저 (Business Discovery는 확실)

- [x] 방향 결정: 친구가 "위험 감수하고 로그인 방식"을 선택 (2026-08-10). 최대한 안전하게 구현하기로.

### 🔥 인스타 핫 탭 구현 (2026-08-10) — insta_hot.py 별도 파일
> 위험 기능이라 다운로드 코드와 완전히 분리(insta_hot.py). 여기가 다 죽어도 다운로드는 됨.
> 새 의존성 없음 — 쿠키는 yt-dlp 내장 기능, 요청은 이미 있는 curl_cffi로.

**안전 설계 (조사에서 나온 "그나마 덜 걸리는" 조건을 코드로 강제):**
- [x] 로그인을 우리가 안 함 — 친구가 브라우저에서 부계정 로그인한 세션 쿠키만 빌림
      (재로그인 시 기기지문 새로 생기는 게 정지 1순위 원인 → 로그인 자체를 안 함)
- [x] 요청 순차(락) + 사이에 2.5~6초 랜덤 지연 + 하루 상한 120회 + 10분 캐시
- [x] challenge/checkpoint 감지 시 그날 통째로 자동 휴식(24h), rate_limited는 12h 휴식
- [x] 세션 만료(로그인 풀림)는 "브라우저에서 다시 로그인" 한국어 안내로 구분
- [x] 화면 상단 빨간 경고: 부계정만·본계정 절대 금지·전화번호/기기 안 엮기·하루 제한

**기능:**
- [x] 브라우저 선택 + 연결 확인(요청 1번으로 세션 검증)
- [x] 벤치마킹 계정(web_profile_info) + 해시태그(tags/web_info, 구조 변화 견디는 재귀 파서)에서
      릴스의 좋아요·댓글·조회·업로드시각 수집
- [x] 가변 기준 필터: 최근 N시간 / 최소 좋아요 / 최소 댓글 / 정렬(시간당좋아요·좋아요·댓글·조회·최신)
      → settings.json에 기억. 친구 예시 "8시간·댓글1000·시간당좋아요순" 확인
- [x] 순위표(썸네일 그리드) + 그 자리 재생(인스타 embed) + 기존 "받기"로 연결
- [x] settings.json 병합 저장(update_settings) — "저장 위치 바꾸기"가 인스타 설정을 안 지우게 함께 수정

**검증:**
- [x] 오프라인: 노드/모바일 파서, 재귀 파서, 가변 기준 필터·순위 정렬 (합성 데이터로 assert 통과)
- [x] 라우트: 미설정 시 안전하게 한국어로 막힘, 이상한 기준값(-5·"abc") 정화, 설정 병합 저장,
      기존 다운로드 status API 회귀 통과
- [x] UI: 인스타 탭 렌더링·경고·설정·기준 화면 (Playwright 스크린샷, JS 에러 없음)
- [ ] ⚠️ **실제 인증 수집은 미검증** — 부계정 세션이 있어야 fetch_account_reels/fetch_hashtag_reels의
      실제 인스타 응답 파싱을 확인 가능. 개발 중 실제 쿠키를 건드리지 않음(본계정일 수 있어서).
      → 친구(또는 개발자)가 부계정 로그인 후 "연결 확인"→"핫 릴스 찾기"로 라이브 검증 필요.
         이때 응답 구조가 예상과 다르면 파서만 소폭 조정하면 됨(안전장치·UI·순위는 이미 검증됨)

### 머닝 데이터 수집 방식 확인 (2026-08-10) — 트래커는 사용자 계정·IP 안 씀
친구 걱정("IP 밴·연쇄 밴이 무섭다")에 답하려고 머닝이 어떻게 긁는지 조사:
- 트렌드/급상승/교차영상(트래커 계열): **사용자 본인 인스타 계정 연동 불필요.** 남의 @아이디·키워드·링크만 입력.
  수집은 **머닝 서버에서** 일어남("매일 오후 1시 자동 수집" = 사용자 접속 안 해도 긁음, 크레딧 과금 = 서버비용 배분).
  확장프로그램은 쿠팡 링크변환 전용이라 트래커와 무관 → 사용자 IP·세션·쿠키 개입 통로 없음.
  → **머닝 사용자의 인스타 계정·IP는 이 기능에서 안전.** 위험을 머닝 서버가 떠안는 구조.
- 계정 연동이 필요한 건 DM 자동화(머닝링크)뿐 — 이건 공식 Graph/Send API + OAuth. 그리고 이쪽은
  약관에 "계정 정지돼도 책임 안 짐" 면책 있음. 친구가 원하는 트래커와는 위험 구조가 완전히 다름.
- 불확실(정직히): 트래커 데이터 출처가 스크래핑인지 공식 API인지 명문 근거 없음(정황상 자체 수집 추정).
  단 그건 *머닝의* ToS 리스크지 *사용자 계정* 노출이 아님. 트래커 밴 후기도 독립 근거 미확보.

**우리 도구와의 결정적 차이:** 우리는 친구 노트북(집 IP)+친구 부계정으로 돌림 = 위험이 친구에게.
머닝은 자기 서버+자기 계정/프록시 = 위험이 머닝에게. 이 "서버+자체계정" 모델은 무료 개인도구로
싸게 복제 불가(클라우드 IP는 인스타가 상시 차단 + 웹배포 안 하기로 이미 결정 + 자체 부계정풀·주거용
프록시 비용). → 머닝급 안전을 원하면 길은 셋뿐: ① 공식 API(Business Discovery) ② 틱톡(tikwm, 로그인0)
③ 이 기능만 머닝 트래커를 계속 쓰기(월 29,000원에 위험을 대신 져줌).
