# auto-downloader

영상편집자 친구를 위한 자동 다운로드 도구. **최종 사용자는 비개발자**다.

## 사용자 원칙
- 실행은 `.command` 파일 더블클릭. 친구에게 터미널 명령을 치게 하지 않는다.
- 화면에 보이는 모든 메시지·에러는 한국어로, "왜 안 됐는지 + 어떻게 하면 되는지"까지 쓴다.
- 실패해도 프로그램이 그냥 죽지 않는다. 실패 항목은 건너뛰고 마지막에 정리해서 보여준다.

## 기술 규칙
- Python 3 + venv, 의존성은 `requirements.txt` (threads-automation 프로젝트와 같은 방식)
- 구조가 단순할 땐 파일 하나로 유지, 커지면 그때 분리한다.
- 결과물(다운로드된 파일)은 git에 넣지 않는다.

## 작업 방식
- 모든 작업은 `SPEC.md`의 할 일 체크박스에서 시작한다. 스펙에 없는 요구는 먼저 스펙에 적는다.
- 기능 하나가 끝나면: 실제로 실행해서 확인 → SPEC.md 체크 → git commit.
- 사용 설명서는 `docs/`에 한국어로 쓴다.
